<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a10_26_2019_loginpage">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".resident.ResidentSupportActivity" />
        <activity android:name=".resident.ResidentDeliveryActivity" />
        <activity android:name=".resident.ResidentHistoryActivity" />
        <activity android:name=".resident.ResidentMainActivity" />
        <activity android:name=".manager.ManagerSupportActivity" />
        <activity android:name=".manager.ManagerDeliveryActivity" />
        <activity android:name=".manager.ManagerHistoryActivity" />
        <activity android:name=".manager.ManagerMainActivity" />
        <activity android:name=".concierge.ConciergeSupportActivity" />
        <activity android:name=".concierge.ConciergeDeliveryActivity" />
        <activity android:name=".concierge.ConciergeHistoryActivity" />
        <activity android:name=".concierge.ConciergeMainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>